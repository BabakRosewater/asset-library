# dist/ (Import-ready assets)

This directory contains approved production assets only.

- No editable masters in `dist/`.
- Deprecated assets should move to `dist/_deprecated/`.
- Keep metadata in `dist/manifest.json` current.
