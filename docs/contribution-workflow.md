# Contribution Workflow

1. Add or edit working files in `src/`.
2. Request review for brand/department owner approval.
3. Export approved files into `dist/` using naming conventions.
4. Update `dist/manifest.json`.
5. Move superseded outputs to `dist/_deprecated/` when replaced.
